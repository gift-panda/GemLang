import Gem.Math;

class Counter{
    var count = 0;
    execute(){
        Counter.count = Counter.count + 1;
        println("executed " + Counter.count + " times");
    }
}

var c = Counter();

c.execute();
c.execute();
c.execute();
c.execute();

var d = Counter();
d.execute();
c.execute();